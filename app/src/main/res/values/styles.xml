<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:spinnerDropDownItemStyle">@style/PageOneDropDown</item>

    </style>
    <style name="PageOneDropDown" parent="@android:style/Widget.Holo.DropDownItem.Spinner">
        <item name="android:textAppearance">@style/PageOneDropDownText</item>
    </style>
    <style name="PageOneDropDownText" parent="@android:style/Widget">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">14sp</item>
        <!-- Here you can set the color and other text attributes -->
    </style>
</resources>
